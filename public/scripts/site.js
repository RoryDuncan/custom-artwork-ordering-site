// Generated by CoffeeScript 1.8.0
var height, item, items, _i, _len;

document.ready = [];

items = document.querySelectorAll('.fullpage');

height = window.innerHeight - 85;

for (_i = 0, _len = items.length; _i < _len; _i++) {
  item = items[_i];
  item.style['min-height'] = "" + height + "px";
}

document.onreadystatechange = function(e) {
  if (document.readyState === "interactive") {
    return document.ready.forEach(function(el) {
      return el.call();
    });
  }
};

document.ready.push(function() {
  var mobilenav, navToggle;
  mobilenav = document.querySelectorAll('.navigation .mobile ul.nav')[0];
  navToggle = document.querySelectorAll('.navigation .mobile .toggle-nav')[0];
  return navToggle.addEventListener("click", function(e) {
    mobilenav.classList.toggle("active");
    navToggle.classList.toggle("active");
    e.preventDefault();
    return landscape.addEventListener("change", invertDimensions);
  });
});
